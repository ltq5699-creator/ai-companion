# 新手部署指南（完全免费 · 零基础也能做）

> 目标：把你手上的 AI 伴侣 App 打包成 APK，装到安卓手机上用。
> 全程免费，不需要任何付费服务。

---

## 一、先说清楚：花钱吗？

**不花钱。** 下面每一项都是免费的：

| 步骤 | 是否免费 | 说明 |
|------|----------|------|
| 注册 GitHub 账号 | ✅ 免费 | 用邮箱即可 |
| 生成 PAT 令牌 | ✅ 免费 | 它只是个「登录凭证」，不是要花钱买的 API |
| GitHub Actions 云端打包 | ✅ 免费 | 公开仓库无限分钟；私人仓库每月 2000 分钟也够用 |
| 手机安装 APK | ✅ 免费 | 直接传文件安装 |

唯一「可能」涉及花费的是 **App 里的 AI 对话需要模型密钥**。但：
- App 内置「零密钥演示模式」，不填任何 Key 也能看界面、收定时消息、体验交互（AI 回复是示例文本）；
- 想要真·AI 对话，Gemini / Deepseek 都有**免费额度**，注册即送，不用花钱。

---

## 二、我已经帮你做好的（你不用管）

- ✅ 整个工程已初始化成 Git 仓库，并完成第一次提交（56 个文件）
- ✅ 已自动排除 `node_modules`、签名文件，已包含 `gradlew` 让云端能直接跑
- ✅ 复查并修好了：云端构建配置、Android 定时消息权限、TypeScript 类型检查

你给到下面两项信息后，我会**全自动**完成：绑定仓库 → 推送代码 → 触发构建 → 把 APK 下载链接发你。你全程不用敲命令。

---

## 三、必须由你本人做的（只需一个免费 GitHub 账号）

### 第 1 步：注册 GitHub（约 1 分钟）
1. 打开 https://github.com
2. 点右上角 **Sign up**
3. 填「用户名 / 邮箱 / 密码」→ 验证邮箱 → 完成

### 第 2 步：新建一个空仓库（约 30 秒）
1. 登录后，点右上角 **+** → **New repository**
2. Repository name 填：`ai-companion`
3. 选 **Public**（公开，构建免费且无限分钟）
4. **不要**勾 Add a README（保持空仓库）
5. 点 **Create repository**

### 第 3 步：生成 PAT 令牌（约 1 分钟）
1. 右上角头像 → **Settings** → 左侧最下面 **Developer settings**
2. → **Personal access tokens** → **Tokens (classic)**
3. 点 **Generate new token (classic)**
4. Note 填：`ai-companion-build`
5. Expiration 选 `7 days`（用完可随时撤销）
6. 勾选 scope：☑ `repo`（子项会自动全勾上）和 ☑ `workflow`
7. 拉到底点 **Generate token**
8. **复制那一长串 token**（只显示这一次！）

### 第 4 步：把这两项发给我
- 你的仓库地址（类似 `https://github.com/你的用户名/ai-companion.git`）
- 刚才复制的 PAT token

我就帮你推代码 + 跑构建 + 给下载链接。
> 安全提示：token 用完随时可撤销（Settings → Developer settings → PAT → 删掉），几秒钟的事；我也只会用它推这一次代码，不会留存。

---

## 四、备选：你想自己推（不把 token 给我）

仓库建好后，在终端粘贴两行（把地址换成你的）：

```bash
git remote add origin https://github.com/你的用户名/ai-companion.git
git push -u origin master
```

然后到 GitHub → **Actions** → **Build Android APK** → **Run workflow**，等几分钟下载 APK。

---

## 五、拿到 APK 后

1. 把 APK 传到手机（微信文件传输助手 / USB / 网盘都行）
2. 手机允许「未知来源」安装
3. 安装 → 打开 → 在「设置」里填模型 Key（或直接用演示模式）

---

## 六、常见问题

**Q：一定要 GitHub 吗？能不能不注册？**
A：云端打包必须有一个代码托管账号，GitHub 是最免费、最简单的。没有它无法在云端出包（你的电脑也没装 JDK/安卓环境，本地出不了）。

**Q：令牌会不会被盗刷扣费？**
A：不会。GitHub 令牌完全免费，没有「扣费」概念；且只授权推代码，不涉及任何支付。

**Q：构建要多久？**
A：首次约 5–10 分钟（要下载依赖和安卓环境），之后几分钟。
